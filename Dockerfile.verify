# 一次性核验服务镜像：代码测试 + 构建检查 + 健康地址冒烟
FROM node:22-alpine
WORKDIR /app
COPY package.json ./
COPY src ./src
COPY tests ./tests
COPY scripts ./scripts
RUN chmod +x scripts/verify.sh
CMD ["sh", "scripts/verify.sh"]
