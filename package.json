{
  "name": "quake-alert-code-workbench",
  "version": "1.0.0",
  "private": true,
  "description": "地震预警短二进制码编配工作台（纯静态前端，无运行时依赖）",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "serve": "node scripts/serve.mjs",
    "verify": "npm test && npm run build"
  }
}
